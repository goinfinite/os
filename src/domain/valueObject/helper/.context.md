<context path="src/domain/valueObject/helper" updated="2026-09-08">

Helper functions for cryptographic string hashing. All type conversion helpers (interfaceToBool, interfaceToString, interfaceToInt64, interfaceToUint, etc.) and regex utilities (findNamedGroupsMatches) were migrated to the tk library.

## Summary

- strongStringHasher.go — SHA3-256 hex string hashing (`StrongStringHasher`) and its truncated variant (`StrongStringShortHasher`); used by sslCertificateId.go, sslPairId.go, and the ssl, marketplace, and services infra repos

## Constraints

- MUST NOT change the digest algorithm or the truncation length: outputs are persisted as identifiers (SSL certificate/pair IDs, marketplace mapping content hashes, service startup-file name suffixes)

## Guidance

- SHOULD pin every digest-behavior change with golden-vector tests in strongStringHasher_test.go

</context>
