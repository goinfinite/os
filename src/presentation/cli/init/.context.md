<context path="src/presentation/cli/init" updated="2026-02-20">

Database service initialization for the CLI. Creates and manages connections to persistent, transient, and trail databases when the CLI starts.

## Summary

- databaseService.go — Database service instantiation
- Called by cli.go:CliInit() before command execution

## Constraints

- MUST initialize all three database services before proceeding
- MUST fail fast if database initialization fails

</context>
