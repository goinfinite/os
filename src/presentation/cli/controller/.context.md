<context path="src/presentation/cli/controller" updated="2026-02-20">

CLI command handlers implementing the same features as API controllers, but as Cobra commands instead of HTTP handlers.

## Summary

- account.go, authentication.go, database.go, etc. — Command implementations for each feature
- Each controller returns Cobra commands ready to be registered in router.go

## Constraints

- MUST implement same business logic as API controllers
- MUST validate flags and arguments before calling use cases

</context>
