<context path="src/presentation/shared/helper" updated="2026-09-05">

Shared helper utilities for cross-presentation-layer operations.

## Summary

- authenticationHelper.go — AuthenticationHelper struct with ExtractAccessToken, ExtractIpAddress, ReadAccountIdFromAccessToken methods; consolidates token-cookie/header/Bearer parsing, IP extraction (via tkPresentation.NewRequesterIpExtractor), and trusted-CIDR account-id lookup shared by API and UI middlewares
- localOperator.go — `LocalOperatorAccountId`/`LocalOperatorIpAddress` defaults: the identity attributed to operations with no authenticated operator (initial setup, local/CLI actions), shared by the API layer and the liaison helpers

</context>
