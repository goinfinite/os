<context path="src/infra/envs" updated="2026-09-11">

Central catalog of filesystem paths, service accounts, and environment keys the infrastructure layer shares.

## Summary

- envs.go — path, account, and environment-key constants for the application directories, web servers, PHP runtime, databases, and process manager

## Constraints

- MUST derive nested paths from the base directory constants (`InfiniteOsMainDir`, `ApplicationRootDir`) instead of repeating literal prefixes
- MUST NOT contain functions or mutable state

</context>
