<context path="src/infra/files" updated="2026-09-11">

File system operations including file upload, download, deletion, and directory management.

## Summary

- File repository implementations
- File I/O operations

## Constraints

- MUST validate file paths for security (no path traversal)
- MUST respect file permissions and ownership

## Guidance

- SHOULD keep `UpdateContent` writes owned by the target's parent directory owner, so files under `/app` stay readable by the web server user. The write passes `FileClerkOwnerSourceContainingDirectory` and trusts the containing-directory owner in the tk directory chain.

</context>
