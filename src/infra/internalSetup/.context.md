<context path="src/infra/internalSetup" updated="2026-07-03">

Container bootstrap and internal setup including runtime directory creation, web server initialization, and NGINX configuration.

## Summary

- Container bootstrap orchestration (ContainerBootstrap)
- Runtime directory setup (primary public dir creation)
- Web server configuration and startup
- NGINX integration and management
- Port and SSL binding

## Constraints

- MUST start web server on correct ports (80, 443, 1618)
- MUST apply SSL certificates correctly
- MUST create /app/html in upper container layer (not build layer) to prevent fuse-overlayfs whiteout accumulation

</context>
